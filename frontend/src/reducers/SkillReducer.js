const initState = {
    skills :{},
    skill: {
        name: '',
        lost: false,
        checked: false,
        owner_id: ''
    }
}

const skillReducer = (state = initState, action) => {
    switch (action.type){
    
    default:
        return state
    }       
}

export default skillReducer